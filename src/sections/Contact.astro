<section id="contact" class="bg-black text-white py-16">
  <div class="container mx-auto px-4 text-center">
    <h2 class="text-3xl font-bold mb-6">
      ¿Dudas en como empezar? ¡No dudes en contactarnos!
    </h2>
    <form class="max-w-lg mx-auto space-y-4">
      <input
        type="text"
        placeholder="Nombre completo"
        class="w-full p-2 rounded"
      />
      <input
        type="email"
        placeholder="Correo electrónico"
        class="w-full p-2 rounded"
      />
      <input type="tel" placeholder="Teléfono" class="w-full p-2 rounded" />
      <input
        type="text"
        placeholder="Horario de recibir llamadas"
        class="w-full p-2 rounded"
      />
      <textarea
        placeholder="Escribe tu mensaje aquí"
        class="w-full p-2 rounded h-24"></textarea>
      <button
        type="submit"
        class="bg-orange-500 px-6 py-3 rounded-full hover:bg-orange-600"
        >Contáctenos</button
      >
    </form>
  </div>
</section>
